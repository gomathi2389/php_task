5. we will have permanent,temporary,current like that. also i may list only permnent address of user or current address of user
TABLE NAME -> USERS , USER_ADDRESSES

CREATE TABLE users (
    user_id INT AUTO_INCREMENT PRIMARY KEY,
    username varchar(20),
    dob DATE,
    age INT
);

CREATE TABLE user_addresses (
    address_id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    street VARCHAR(100) NOT NULL,
    city VARCHAR(100) NOT NULL,
    state VARCHAR(50) NOT NULL,
    type ENUM('permanet','current','temporary'),
    FOREIGN KEY (user_id) REFERENCES users(user_id)
)

//if i want particular user details of address
query-> SELECT * FROM user_addresses WHERE user_id = 1 AND type = "permanent";
//if i want all user permannet address
query-> SELECT * FROM user_addresses WHERE user_id = 1 AND type = "permanent";

//7th and 8th highest mark and student details mysql query





